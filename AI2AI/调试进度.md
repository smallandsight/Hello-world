# 调试进度

> **项目**：古月租车小程序（gy_bike）
> **创建日期**：2026-04-05
> **当前阶段**：第一阶段（W1-4）后端编码完成 — 待启动调试
> **对应源码**：`Basic/server/src/`（89 个 .ts 文件）

---

## 一、调试总览

```
调试状态  ████████████░░░░░░░░░░░░░  准备中：0% | 待启动
```

| 维度 | 状态 | 说明 |
|------|------|------|
| **环境准备** | ⬜ 未开始 | npm install / .env 配置 / MySQL 连接 / Redis 连接 |
| **编译验证** | ⬜ 未开始 | `npm run build` 检查 TypeScript 编译是否通过 |
| **启动验证** | ⬜ 未开始 | `npm run start:dev` 检查应用能否正常启动 |
| **单元测试** | ⬜ 未开始 | 对应 `AI2AI/测试用例.md` 中 93 个测试用例 |
| **联调验证** | ⬜ 未开始 | Swagger UI 访问 + 接口手动测试 |

---

## 二、分模块调试计划

### 🔧 前置步骤（必须先完成）

| 步骤ID | 调试项 | 操作命令 | 预期结果 | 状态 |
|--------|-------|---------|---------|------|
| PRE-01 | 安装依赖 | `cd Basic/server && npm install` | node_modules 生成，无 peer dependency 冲突 | ⬜ |
| PRE-02 | 配置环境变量 | 复制 `.env.example` → `.env`，填入本地 MySQL/Redis/JWT 配置 | `.env` 文件就绪 | ⬜ |
| PRE-03 | 验证 MySQL 连接 | 确保 MySQL 服务运行，数据库已创建 | mysql -u root -p 可连接 | ⬜ |
| PRE-04 | 验证 Redis 连接 | 确保 Redis 服务运行 | redis-cli ping 返回 PONG | ⬜ |
| PRE-05 | TypeScript 编译检查 | `npm run build` | dist/ 目录生成，**零编译错误** | ⬜ |
| PRE-06 | 启动开发服务器 | `npm run start:dev` | 终端输出 `Application is running on: http://localhost:3000` | ⬜ |
| PRE-07 | 访问 Swagger 文档 | 浏览器打开 `http://localhost:3000/api-docs` | Swagger UI 页面正常显示 API 列表 | ⬜ |

---

### M1: Common 公共基础设施

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| HttpExceptionFilter 异常捕获 | `filters/http-exception.filter.ts` | 发送非法请求触发 400/404/500 | 返回统一 `{code, message, data, timestamp}` 格式 | | ⬜ |
| QueryExceptionFilter 数据库异常 | `filters/query-exception.filter.ts` | 故意构造重复唯一键插入 | 返回友好提示而非 SQL 错误堆栈 | | ⬜ |
| ResponseInterceptor 自动包装 | `interceptors/response.interceptor.ts` | 调用任意成功接口 | 响应体自动包含 code=0 + timestamp | | ⬜ |
| LoggingInterceptor 日志输出 | `interceptors/logging.interceptor.ts` | 观察终端日志 | 每个请求输出 method/url/statusCode/duration/ip | | ⬜ |
| JwtAuthGuard Token 认证 | `guards/jwt-auth.guard.ts` | 无 Token 访问受保护接口 → 有效 Token 访问 | 401 → 200，黑名单 Token 被拒 | | ⬜ |
| StaffAuthGuard 商家认证 | `guards/staff-auth.guard.ts` | 使用 staff 端 Token 访问商家接口 | userType='staff' 的 Token 通过 | | ⬜ |
| RolesGuard RBAC 权限 | `guards/roles.guard.ts` | operator 角色访问 @Roles('admin') 接口 | 403 Forbidden | | ⬜ |
| RedisService 基础操作 | `redis.service.ts` | 在 Swagger 或单元测试中调用 set/get/del | 读写正常，TTL 生效 | | ⬜ |
| RedisService 分布式锁 | `redis.service.ts` | acquireLock 同一 key 两次 | 第一次 true，第二次 false | | ⬜ |
| RedisService 限流 | `redis.service.ts` | rateLimit 快速连续请求超过阈值 | allowed=false | | ⬜ |
| PageQueryDto 分页参数校验 | `dto/pagination.dto.ts` | 传入 page=0, size=999 | 自动修正为边界值 | | ⬜ |

### M2: Auth 认证模块

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| JWT 签发 — 用户端 | `auth.service.ts` signUserTokens | 调用 POST /auth/alipay/login（或直接测 Service） | 返回 accessToken + refreshToken + expiresAt | | ⬜ |
| JWT 签发 — 商家端 | `auth.service.ts` signStaffTokens | 调用 POST /auth/staff/login | accessToken 过期时间约 2 天 | | ⬜ |
| Token 刷新 | `auth.service.ts` refreshUserToken | POST /auth/refresh {refreshToken} | 返回全新 TokenPair | | ⬜ |
| RefreshToken 类型校验 | `auth.service.ts` refreshUserToken | 用 accessToken 当 refreshToken 传 | 抛出 '无效的 RefreshToken' | | ⬜ |
| 登出黑名单 | `auth.service.ts` logout | POST /auth/logout (需认证) | 再次用该 Token 访问接口被拒 | | ⬜ |
| DTO 校验 — 缺少必填字段 | `auth/dto/*.ts` | POST /auth/alipay/login {} (空 body) | 400 参数校验失败 | | ⬜ |

### M3: User 用户模块

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| 获取用户信息 | `user.controller.ts` + `user.service.ts` | GET /user/info（带有效 JWT） | 返回用户对象（敏感字段 select:false 不出现） | | ⬜ |
| 用户不存在 | `user.service.ts` getUserInfo | 伪造 userId | 404 '用户不存在' | | ⬜ |
| 更新用户信息 | `user.controller.ts` | PUT /user/info {nickname:'新昵称'} | 更新成功并返回新数据 | | ⬜ |
| UpdateUserDto 校验 | `dto/update-user.dto.ts` | nickname 传超长字符串(>50) | 400 校验失败 | | ⬜ |

### M4: Vehicle 车辆模块

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| 车辆列表（公开访问） | `vehicle.controller.ts` | GET /vehicle/list 无 Token | 200 OK（@Public() 生效） | | ⬜ |
| 车辆详情含关联 | `vehicle.service.ts` getVehicleDetail | GET /vehicle/:id/detail | 含 store/model/images 关联数据 | | ⬜ |
| 车型列表 | `vehicle.service.ts` getModelList | GET /vehicle/model/list | 仅返回 isActive=1 的车型 | | ⬜ |
| 门店列表分页 | `vehicle.service.ts` getStores | GET /vehicle/store/list?page=1&size=5 | 返回分页结果 | | ⬜ |

### M5: Order 订单模块（重点）

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| 创建订单 | `order.service.ts` createOrder | POST /order {vehicleId,storeId,...} | status=PENDING_PAYMENT(10) | | ⬜ |
| 订单列表分页+筛选 | `order.service.ts` getOrderList | GET /order/list?status=10&page=1&size=10 | 返回 {list,total} 结构 | | ⬜ |
| 订单详情关联查询 | `order.service.ts` getOrderDetail | GET /order/:id | 含 user/vehicle/store 关联 | | ⬜ |
| 取消订单 — 合法状态 | `order.service.ts` cancelOrder | POST /order/:id/cancel (status=10待支付) | status→CANCELLED(70) | | ⬜ |
| 取消订单 — 非法状态 | `order.service.ts` cancelOrder | 尝试取消 status=30使用中 的订单 | 返回 {code:4002,'当前状态无法取消'} | | ⬜ |
| canTransition 状态机 | `shared/types/order.types.ts` | 单元测试遍历所有状态转移组合 | 与文档状态机图完全一致 | | ⬜ |

### M6: Payment 支付模块

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| 创建支付（骨架） | `payment.service.ts` | POST /payment/pay | 返回待实现占位信息 | | ⬜ |
| 支付状态查询 | `payment.service.ts` getPaymentStatus | GET /payment/:orderId/status | 返回 Payment 或 null | | ⬜ |
| Entity 字段完整性 | `entities/payment.entity.ts` 等 | 检查 3 张表(Payment/Preauth/Refund) 字段对齐 DDL | 字段名/类型/约束一致 | | ⬜ |

### M7: Coupon 营销模块

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| 我的优惠券（骨架） | `coupon.service.ts` | GET /coupon/my/list | 返回待实现占位 | | ⬜ |
| 积分余额（骨架） | `coupon.service.ts` | GET /coupon/points/balance | 返回待实现占位 | | ⬜ |
| Entity 关联完整性 | `entities/*.entity.ts` | 检查 Coupon→UserCoupon→PointsRecord 外键关系 | OneToMany/ManyToOne 正确 | | ⬜ |

### M8: Ticket 工单模块

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| 提交工单（骨架） | `ticket.service.ts` | POST /ticket | 返回待实现占位 | | ⬜ |
| 工单回复（骨架） | `ticket.service.ts` | POST /ticket/:id/reply | 返回待实现占位 | | ⬜ |

### M9: Staff 商家权限模块

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| RBAC 角色拦截 | `staff.controller.ts` | operator Token 访问 GET /staff/list(@Roles('admin')) | 403 Forbidden | | ⬜ |
| RBAC 权限拦截 | `staff.controller.ts` | 无 role:view 权限访问 GET /staff/role/list | 403 Forbidden | | ⬜ |
| 多对多关系完整性 | `entities/*.entity.ts` | 检查 Staff↔Role↔Permission 中间表结构 | RolePermission/StaffRole 正确 | | ⬜ |

### M10: Message 消息模块

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| 消息列表（骨架） | `message.service.ts` | GET /message/list | 返回待实现占位 | | ⬜ |
| 标记已读（骨架） | `message.service.ts` | PUT /message/:id/read | 返回待实现占位 | | ⬜ |

### M11: Shared Types 共享类型

| 调试项 | 文件 | 调试方法 | 预期行为 | 问题记录 | 状态 |
|--------|------|---------|---------|---------|------|
| OrderStatus 枚举完整性 | `order.types.ts` | 代码审查 | 8 种状态值与开发计划一致(10-80) | | ⬜ |
| 状态机转换规则 | `order.types.ts` | 单元测试 canTransition() | 所有合法/非法转移正确 | | ⬜ |
| IPayload/StaffPayload 类型 | `user.types.ts` | TypeScript 编译检查 | 所有引用处类型匹配无误 | | ⬜ |

---

## 三、已知待解决问题（编码时预留的 TODO）

以下为代码中的 TODO 标记汇总，需要在后续迭代中实现：

| TODO ID | 所在文件 | 内容描述 | 优先级 | 所属阶段 |
|---------|---------|---------|--------|---------|
| TODO-01 | `auth.controller.ts` alipayLogin | 支付宝 code → sessionKey → user 查询/创建 | P0 | W5（MVP核心） |
| TODO-02 | `auth.controller.ts` staffLogin | 商家账号密码验证逻辑 | P0 | W8 |
| TODO-03 | `auth.module.ts` | 注入 UserModule / StaffModule（用于登录查询用户） | P0 | W5 |
| TODO-04 | `user.service.ts` getUserInfo | 返回 UserInfoVO（手机号等敏感字段脱敏） | P1 | W6 |
| TODO-05 | `user.service.ts` getMemberInfo | 查询用户等级+积分+升级所需条件 | P1 | W11（营销） |
| TODO-06 | `vehicle.service.ts` getNearbyVehicles | 实现基于经纬度的 LBS 附近车辆查询 | P0 | W5 |
| TODO-07 | `vehicle.service.ts` getVehicleDetail | 脱敏处理 + 格式化返回 | P1 | W6 |
| TODO-08 | `vehicle.service.ts` getStores | 支持坐标范围筛选 | P1 | W8 |
| TODO-09 | `order.service.ts` createOrder | 校验车辆可用性 + 创建订单 + 写日志 | P0 | W6 |
| TODO-10 | `order.service.ts` cancelOrder | 更新状态 + 写 OrderLog | P0 | W7 |
| TODO-11 | `order.service.ts` pickupVehicle | 状态机校验→更新IN_USE→写日志 | P0 | W7 |
| TODO-12 | `order.service.ts` returnVehicle | 状态机校验→更新RETURN_PENDING→写日志 | P0 | W7 |
| TODO-13 | `order.service.ts` previewSettlement | 调用 PricingService 计算结算价格 | P0 | W7 |
| TODO-14 | `payment.service.ts` createPayment | 对接支付宝支付 API | P0 | W6 |
| TODO-15 | `payment.service.ts` requestRefund | 退款流程（支付宝 refund API） | P1 | W10 |
| TODO-16~18 | coupon/ticket/message service | 全部业务逻辑实现 | P1 | W9-W14 |

---

## 四、问题记录模板

当调试过程中发现问题时，按以下格式追加记录：

### 问题 #NN

**日期**：YYYY-MM-DD HH:MM  
**模块**：[模块名称]  
**文件**：[具体文件路径]  
**现象**：[描述错误表现]  
**原因分析**：[初步判断的根因]  
**解决方案**：[采取的措施]  
**解决状态**：⬜ 待解决 / ✅ 已解决 / 🔶 部分解决 / ⏸️ 延后处理  
**影响范围**：[影响哪些功能/接口]

---

*本文档由 AI 于 2026-04-05 创建。每次调试会话开始前必须阅读本文档，调试过程中实时更新进度和问题记录。*
