# 测试用例

> **项目**：古月租车小程序（gy_bike）
> **创建日期**：2026-04-05
> **当前阶段**：第一阶段（W1-4）后端编码 — 单元测试用例
> **对应源码**：`Basic/server/src/`（89 个 .ts 文件）

---

## 一、测试覆盖总览

| 模块 | 优先级 | 源文件数 | 测试用例数 | 覆盖重点 |
|------|--------|---------|-----------|---------|
| **common（公共基础设施）** | P0 | 23 | 28 | 守卫/过滤器/拦截器/DTO/Redis |
| **auth（认证模块）** | P0 | 7 | 16 | JWT签发/刷新/黑名单 |
| **user（用户模块）** | P0 | 8 | 8 | CRUD/脱敏/DTO校验 |
| **vehicle（车辆模块）** | P1 | 10 | 6 | 列表/详情/门店/车型 |
| **order（订单模块）** | P0 | 9 | 10 | 创建/状态机/分页/取消 |
| **payment（支付模块）** | P1 | 6 | 5 | 支付记录/退款/状态查询 |
| **coupon（营销模块）** | P1 | 5 | 4 | 优惠券/积分 |
| **ticket（工单模块）** | P1 | 4 | 4 | 提交/回复/列表/详情 |
| **staff（商家权限模块）** | P1 | 9 | 5 | 角色/员工/RBAC |
| **message（消息模块）** | P2 | 3 | 3 | 已读/列表/未读数 |
| **shared/types（共享类型）** | P0 | 5 | 4 | 枚举/状态机/类型定义 |
| **合计** | — | **89** | **93** | — |

---

## 二、Common 公共基础设施测试（28 例）

### 2.1 JWT 认证守卫 — `JwtAuthGuard`（6 例）

| 用例ID | 测试项 | 输入条件 | 预期结果 | 优先级 |
|--------|-------|---------|---------|--------|
| C-G-01 | 公开接口放行 | `@Public()` 标记的路由，无 Token | `canActivate = true`，不抛异常 | P0 |
| C-G-02 | 缺少 Authorization 头 | 请求头无 Authorization | 抛出 `UnauthorizedException('请先登录')` | P0 |
| C-G-03 | Token 格式错误 | `Authorization: InvalidToken xxx`（非 Bearer） | 抛出 `UnauthorizedException('请先登录')` | P0 |
| C-G-04 | Token 过期或无效 | Bearer + 签名错误的 Token | 抛出 `UnauthorizedException('登录已过期，请重新登录')` | P0 |
| C-G-05 | Token 在黑名单中 | 有效 Token 但 Redis 黑名单 key 存在 | 抛出 `UnauthorizedException('登录已失效，请重新登录')` | P0 |
| C-G-06 | 非用户端 Token 被拒绝 | userType='staff' 的有效 Token | 抛出 `UnauthorizedException('Token 类型不正确')` | P0 |
| C-G-07 | 正常认证通过 | 有效的用户端 JWT（userType='user'） | `request.user = payload`，返回 `true` | P0 |

### 2.2 商家认证守卫 — `StaffAuthGuard`（4 例）

| 用例ID | 测试项 | 输入条件 | 预期结果 | 优先级 |
|--------|-------|---------|---------|--------|
| C-S-01 | 公开接口放行 | `@Public()` 标记的路由 | `canActivate = true` | P1 |
| C-S-02 | 缺少/无效 Token | 无 Authorization 或格式错误 | 抛出 `UnauthorizedException('请先登录商家账号')` | P1 |
| C-S-03 | Token 过期 | 过期的商家端 JWT | 抛出 `UnauthorizedException('商家登录已过期，请重新登录')` | P1 |
| C-S-04 | 正常商家认证 | 有效的 staff 端 Token（userType='staff', roles=['admin']） | `request.user = StaffPayload`，返回 `true` | P1 |

### 2.3 RBAC 权限守卫 — `RolesGuard`（5 例）

| 用例ID | 测试项 | 输入条件 | 预期结果 | 优先级 |
|--------|-------|---------|---------|--------|
| C-R-01 | 无角色/权限要求放行 | 未标注 @Roles/@Permissions 的路由 | 返回 `true` | P1 |
| C-R-02 | 角色匹配通过 | `@Roles('admin')`, user.roles=['admin','manager'] | 返回 `true` | P0 |
| C-R-03 | 角色不匹配 | `@Roles('admin')`, user.roles=['operator'] | 抛出 `ForbiddenException('需要角色: admin, 当前角色: operator')` | P0 |
| C-R-04 | 权限匹配通过 | `@Permissions('order:view')`, user.permissions=['order:view','order:create'] | 返回 `true` | P0 |
| C-R-05 | 角色和权限同时校验 | `@Roles('admin')+@Permissions('staff:view')` 均匹配 | 返回 `true` | P1 |
| C-R-06 | 无 request.user 时 | req.user 为 undefined | 返回 `false`（不抛异常，由上层 Guard 处理） | P2 |

### 2.4 异常过滤器 — `HttpExceptionFilter`（5 例）

| 用例ID | 测试项 | 输入异常 | 预期响应 | 优先级 |
|--------|-------|---------|---------|--------|
| C-F-01 | 标准 HttpException | `new NotFoundException('用户不存在')` | `{code: NOT_FOUND, message: '用户不存在', status: 404}` | P0 |
| C-F-02 | DTO 校验失败 | class-validator 校验失败的 422 异常（message 为数组） | `{code: INVALID_PARAMS, message: '参数校验失败', data: {errors: [...]}}` | P0 |
| C-F-03 | 未知 Error | 普通 Error 对象（非 HttpException） | `{code: SERVER_INTERNAL_ERROR, message: '服务器内部错误', status: 500}` | P0 |
| C-F-04 | 自定义错误码 | 带 `code` 属性的 HttpException | 使用异常自带的 code 和 message | P1 |
| C-F-05 | 日志分级记录 | 500 错误 / 400 错误 | 分别调用 logger.error / logger.warn | P1 |

### 2.5 TypeORM 查询异常过滤器 — `QueryExceptionFilter`（4 例）

| 用例ID | 测试项 | 输入异常 | 预期响应 | 优先级 |
|--------|-------|---------|---------|--------|
| C-Q-01 | 实体未找到 | `EntityNotFoundError` | `{code: NOT_FOUND, status: 404}` | P0 |
| C-Q-02 | 唯一约束冲突 | `QueryFailedError` 含 'Duplicate entry' | `{code: USER_ALREADY_EXISTS, message: '数据已存在', status: 409}` | P0 |
| C-Q-03 | 外键约束违反 | 含 'a foreign key constraint' | `{message: '关联数据不存在', status: 400}` | P1 |
| C-Q-04 | 数据库连接丢失 | 含 'ECONNREFUSED' | `{code: SYSTEM_BUSY, message: '数据库连接异常', status: 503}` | P1 |
| C-Q-05 | 字段超长 | 含 'Data too long' | `{message: '数据长度超限', status: 400}` | P2 |

### 2.6 统一响应拦截器 — `ResponseInterceptor`（2 例）

| 用例ID | 测试项 | 输入数据 | 预期输出 | 优先级 |
|--------|-------|---------|---------|--------|
| C-I-01 | 自动包装普通数据 | Controller 返回 `{id: 1, name: 'test'}` | `{code: 0, message: '操作成功', data: {id:1,name:'test'}, timestamp: ISO}` | P0 |
| C-I-02 | 已是 ApiResponse 格式透传 | 返回含 code+timestamp 的对象 | 不重复包装，原样返回 | P0 |
| C-I-03 | null/undefined 处理 | Controller 返回 null | `{code: 0, data: null}` | P2 |

### 2.7 DTO 校验 — 分页与响应（3 例）

| 用例ID | 测试项 | 输入 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| C-D-01 | PageQueryDto 默认值 | 不传参数 | `page=1, size=20, offset=0, sortFields=[]` | P0 |
| C-D-02 | PageQueryDto 边界值 | `page=0, size=200` | page 被 Min(1) 拦截 / size 被 Max(100) 拦截 | P0 |
| C-D-03 | PageQueryDto 排序解析 | `sort='-createdAt,price'` | `sortFields=[{field:'createdAt',order:'DESC'},{field:'price',order:'ASC'}]` | P1 |

### 2.8 Redis 服务核心方法（6 例）

> ⚠️ 需要 Mock ioredis 或使用内存 Redis（如 redis-memory-server）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| C-RD-01 | set/get 基本读写 | `set('k','v',60)` → `get('k')` | 返回 `'v'`，TTL ≈ 60s | P0 |
| C-RD-02 | get 不存在的键 | `get('nonexist')` | 返回 `null` | P0 |
| C-RD-03 | JSON 序列化/反序列化 | `set('obj',{a:1})` → `get('obj')` | 返回 `{a:1}`（自动 parse） | P0 |
| C-RD-04 | 分布式锁 acquireLock | `acquireLock('lock:1','owner1',30)` → 再次调用 | 第一次 `true`，第二次 `false`（NX） | P0 |
| C-RD-05 | 分布式锁 releaseLock | 持有锁时 `releaseLock('lock:1','owner1')` | Lua 脚本返回 `1`，删除成功 | P0 |
| C-RD-06 | releaseLock 非持有者 | 其他 owner 调用 releaseLock | 返回 `false`（Lua 判断 value 不匹配） | P1 |
| C-RD-07 | 滑动窗口限流正常 | `rateLimit('u:1',60,5)` 连续请求 5 次 | 前 5 次 `allowed=true`，第 6 次 `allowed=false` | P0 |
| C-RD-08 | 限流窗口重置 | 等 60s 后再次调用 rateLimit | 窗口清零，重新计数 | P1 |
| C-RD-09 | incr/decr 数值操作 | `incr('counter')` × 3 → `decr('counter')` | 最终值为 2 | P2 |

---

## 三、Auth 认证模块测试（16 例）

### 3.1 AuthService — JWT 签发与刷新（8 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| A-S-01 | 用户端 Token 签发 | `signUserTokens(123)` | 返回 `{accessToken, refreshToken, expiresAt}`，accessToken 可解码出 sub='123', userType='user' | P0 |
| A-S-02 | 商家端 Token 签发 | `signStaffTokens(1, ['admin'], ['order:view'])` | Payload 含 roles=['admin'], permissions=['order:view'] | P0 |
| A-S-03 | AccessToken 解析过期时间 | 签发后 decode accessToken | `expiresAt` 接近 `Date.now() + 7天(ms)` | P1 |
| A-S-04 | RefreshToken 类型标记 | decode refreshToken | `payload.type === 'refresh'` | P0 |
| A-S-05 | Refresh 成功刷新 | `refreshUserToken(有效的refreshToken)` | 返回全新的 TokenPair | P0 |
| A-S-06 | RefreshToken 过期 | 传入过期或篡改的 token | 抛出 `UnauthorizedException('RefreshToken 已失效')` | P0 |
| A-S-07 | 非 refresh 类型 Token 刷新 | 用 accessToken 调用 refreshUserToken | 抛出 `UnauthorizedException('无效的 RefreshToken')` | P0 |
| A-S-08 | 用户端/商家端路由分发 | user 类型的 refreshToken → signUserTokens; staff 类型 → signStaffTokens | 分别正确分发到对应签发方法 | P0 |
| A-S-09 | 无效 userType 刷新 | 手工构造 userType='unknown' 的 token | 抛出 `UnauthorizedException('无效的 Token 类型')` | P2 |

### 3.2 AuthService — 登出黑名单（3 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| A-L-01 | 正常登出写入黑名单 | `logout(100,'user','validToken')` | Redis 中 `auth:blacklist:100` 存在，TTL ≥ 60s | P0 |
| A-L-02 | 商家端登出 Key 区分 | `logout(200,'staff','token')` | Key 为 `auth:blacklist:staff:200` | P0 |
| A-L-03 | 已过期 Token 登出 | 传入已过期的 token | 直接 return（不写 Redis），无报错 | P1 |

### 3.3 AuthController 路由层（3 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| A-C-01 | 支付宝登录（骨架） | `POST /auth/alipay/login {code:'xxx'}` | 返回 `{message:'待实现：支付宝登录逻辑', data:{code:'xxx'}}` | P1 |
| A-C-02 | 商家登录（骨架） | `POST /auth/staff/login {account:'a',password:'p'}` | 返回待实现占位信息 | P1 |
| A-C-03 | 登出需认证 | `POST /auth/logout` 无 Token | 401 Unauthorized（JwtAuthGuard 拦截） | P0 |

### 3.4 Auth DTO 校验（2 例）

| 用例ID | 测试项 | 输入 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| A-D-01 | AlipayLoginDto code 必填 | `{inviteCode:'abc'}`（缺 code） | 校验失败：`授权码不能为空` | P0 |
| A-D-02 | StaffLoginDto 字段必填 | `{}`（空对象） | 校验失败：账号不能为空 + 密码不能为空 | P0 |
| A-D-03 | RefreshTokenDto 必填 | `{}` | 校验失败：`RefreshToken 不能为空` | P0 |

---

## 四、User 用户模块测试（8 例）

### 4.1 UserService（4 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| U-S-01 | 获取用户信息成功 | `getUserInfo(存在userId)` | 返回 User 对象（不含敏感字段 select:false 的字段） | P0 |
| U-S-02 | 用户不存在 | `getUserInfo(99999)` | 抛出 `NotFoundException('用户不存在')` | P0 |
| U-S-03 | 更新用户信息 | `updateUserInfo(userId, {nickname:'新昵称'})` | 更新成功，返回更新后的 User 对象 | P1 |
| U-S-04 | 会员信息（骨架） | `getMemberInfo(userId)` | 返回 `{message:'待实现：会员信息查询'}` 占位 | P2 |

### 4.2 User DTO 校验（2 例）

| 用例ID | 测试项 | 输入 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| U-D-01 | nickname 超长 | `{nickname: 'x'.repeat(51) }` | 校验失败：MaxLength(50) | P1 |
| U-D-02 | gender 枚举范围 | `{gender: 3 }` | 当前未做枚举限制（⚠️ 待补充 @IsIn([0,1,2])） | P2 |

### 4.3 User Entity 字段验证（2 例）

| 用例ID | 测试项 | 验证内容 | 优先级 |
|--------|-------|---------|--------|
| U-E-01 | User Entity 字段完整性 | 29 字段对齐 DDL：alipayUserId/phone/name/idCard(select:false)/status/isVerified/driverLicense(OneToOne)/userLevel(OneToOne) | P0 |
| U-E-02 | DriverLicense Entity 审核流程 | 16 字段：status/rejectReason/reviewedAt + OCR 字段正副页 | P1 |

---

## 五、Vehicle 车辆模块测试（6 例）

### 5.1 VehicleService（4 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| V-S-01 | 车辆详情含关联 | `getVehicleDetail(存在vehicleId)` | 返回 Vehicle 含 store/model/images 关联 | P0 |
| V-S-02 | 车辆不存在 | `getVehicleDetail(不存在id)` | 返回 null | P1 |
| V-S-03 | 车型列表（仅上架） | `getModelList()` | 仅返回 isActive=1 的 VehicleModel，按 sortOrder ASC | P0 |
| V-S-04 | 门店列表（骨架） | `getStores({page:1, size:10})` | 返回 isActive=1 的 Store 列表 | P1 |

### 5.2 Vehicle 路由公开访问（2 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| V-C-01 | 车辆列表无需认证 | `GET /vehicle/list` 无 Token | 200 OK（@Public() 生效） | P0 |
| V-C-02 | 车辆详情无需认证 | `GET /vehicle/1/detail` 无 Token | 200 OK | P0 |

---

## 六、Order 订单模块测试（10 例）

### 6.1 OrderService（6 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| O-S-01 | 创建订单（骨架） | `createOrder(userId, dto)` | 返回 Order 对象，status=PENDING_PAYMENT(10) | P0 |
| O-S-02 | 订单列表分页 | `getOrderList(userId, {page:1,size:10})` | 返回 `{list:[], total:0}` 结构 | P0 |
| O-S-03 | 按状态筛选订单 | `getOrderList(userId, {status:10})` | WHERE 条件追加 `status = 10` | P0 |
| O-S-04 | 订单详情 | `getOrderDetail(存在orderId)` | 返回 Order 含 user/vehicle/store 关联 | P0 |
| O-S-05 | 订单不存在 | `getOrderDetail(不存在id)` | 抛出 `NotFoundException('订单不存在')` | P0 |
| O-S-06 | 取消订单 — 状态机合法 | `cancelOrder(id, userId)` 当前 status=PENDING_PAYMENT(10) | 更新为 CANCELLED(70)，设置 cancelledAt | P0 |
| O-S-07 | 取消订单 — 状态机非法 | 当前 status=IN_USE(30) 尝试取消 | 返回 `{code:4002, message:'当前状态无法取消'}`（非 canTransition） | P0 |

### 6.2 订单状态机（3 例）

| 用例ID | 测试项 | from → to | 预期结果 | 优先级 |
|--------|-------|-----------|---------|--------|
| O-ST-01 | 合法转移：待支付→待取车 | `canTransition(PENDING_PAYMENT, PICKUP_PENDING)` | `true` | P0 |
| O-ST-02 | 合法转移：使用中→待还车 | `canTransition(IN_USE, RETURN_PENDING)` | `true` | P0 |
| O-ST-03 | 非法转移：已完成→使用中 | `canTransition(COMPLETED, IN_USE)` | `false` | P0 |
| O-ST-04 | 终态不可转移 | `canTransition(CANCELLED, PICKUP_PENDING)` | `false`（终态数组为空） | P1 |
| O-ST-05 | 异常→结算/取消 | `canTransition(ABNORMAL, SETTLING)` → `true`; `canTransition(ABNORMAL, CANCELED)` → `true` | P1 |

### 6.3 Order 路由认证（1 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| O-C-01 | 所有订单接口需认证 | `POST /order` 无 Token | 401 Unauthorized（JwtAuthGuard） | P0 |

---

## 七、Payment 支付模块测试（5 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| P-S-01 | 创建支付（骨架） | `createPayment({orderId:1, amount:10000})` | 返回待实现占位 | P1 |
| P-S-02 | 支付状态查询 | `getPaymentStatus(存在orderId)` | 返回 Payment 对象或 null | P0 |
| P-S-03 | 申请退款（骨架） | `requestRefund(orderId, '不想租了')` | 返回待实现占位 | P2 |
| P-C-01 | 支付接口需认证 | `POST /payment/pay` 无 Token | 401 | P0 |
| P-E-01 | Payment Entity 字段完整性 | 18 字段：tradeNo/channel(租金/押金/违章)/amount/status/refundStatus | P1 |

---

## 八、Coupon 营销模块测试（4 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| CO-S-01 | 我的优惠券（骨架） | `getUserCoupons(userId)` | 返回待实现占位 | P2 |
| CO-S-02 | 订单可用优惠券（骨架） | `getAvailableForOrder(userId, orderId)` | 返回待实现占位 | P2 |
| CO-S-03 | 积分余额（骨架） | `getPointsInfo(userId)` | 返回待实现占位 | P2 |
| CO-E-01 | Coupon/UserCoupon/PointsRecord Entity | 3 张表：优惠券模板(18字段) + 用户领券(13字段) + 积分流水(8字段) | P1 |

---

## 九、Ticket 工单模块测试（4 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| T-S-01 | 提交工单（骨架） | `createTicket(userId, {type:'投诉', content:'xxx'})` | 返回待实现占位 | P2 |
| T-S-02 | 工单列表（骨架） | `getTicketList(userId, {})` | 返回待实现占位 | P2 |
| T-S-03 | 工单详情（骨架） | `getTicketDetail(ticketId)` | 返回待实现占位 | P2 |
| T-S-04 | 回复工单（骨架） | `replyToTicket(ticketId, userId, '好的')` | 返回待实现占位 | P2 |

---

## 十、Staff 商家权限模块测试（5 例）

### 10.1 RBAC 集成（3 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| ST-C-01 | 员工信息（需 StaffAuthGuard） | `GET /staff/profile` 无 Token | 401 | P1 |
| ST-C-02 | 角色列表需权限 | `GET /staff/role/list`（@Permissions('role:view')） | 无权限时 403 Forbidden | P0 |
| ST-C-03 | 员工列表需管理员角色 | `GET /staff/list`（@Roles('admin','manager')） | operator 角色访问被拒 403 | P0 |

### 10.2 Staff Entity（2 例）

| 用例ID | 测试项 | 验证内容 | 优先级 |
|--------|-------|---------|--------|
| ST-E-01 | RBAC 5 张表结构 | Role(8字段) + Permission(8字段) + Staff(14字段) + RolePermission(3) + StaffRole(3) | P1 |
| ST-E-02 | 密码存储安全 | Staff Entity password 字段 select:false | P0 |

---

## 十一、Message 消息模块测试（3 例）

| 用例ID | 测试项 | 操作 | 预期结果 | 优先级 |
|--------|-------|------|---------|--------|
| M-S-01 | 消息列表（骨架） | `getMessageList(userId, {page:1})` | 返回待实现占位 | P2 |
| M-S-02 | 标记已读（骨架） | `markAsRead(messageId)` | 返回待实现占位 | P2 |
| M-S-03 | 未读数量（骨架） | `getUnreadCount(userId)` | 返回待实现占位 | P2 |

---

## 十二、Shared Types 共享类型测试（4 例）

| 用例ID | 测试项 | 验证内容 | 预期结果 | 优先级 |
|--------|-------|---------|---------|--------|
| SH-T-01 | OrderStatus 枚举值 | 8 个状态：10/20/30/40/50/60/70/80 | 枚举完整，值连续无遗漏 | P0 |
| SH-T-02 | ORDER_STATUS_LABELS 映射 | 每个枚举值都有中文 label | 无 undefined label | P0 |
| SH-T-03 | canTransition 函数 | 所有合法/非法组合遍历 | 与文档中的状态机图一致 | P0 |
| SH-T-04 | IPayload / StaffPayload 类型 | 字段完整性：sub/userType/(roles+permissions)/(alipayUserId+phone+nickname) | P1 |
| SH-T-05 | VehicleStatusEnum | 可用/出租中/维护中/已下架/已删除 | P1 |

---

## 十三、集成测试场景（高优先级）

以下为核心业务流程的端到端测试场景，建议在单元测试通过后补充：

| 场景ID | 场景名称 | 涉及步骤 | 优先级 |
|--------|---------|---------|--------|
| E2E-01 | 完整登录→认证→访问流程 | 支付宝登录(模拟) → 签发JWT → 携带Token访问受保护接口 → 响应格式统一 | P0 |
| E2E-02 | 订单生命周期状态流转 | 创建(10)→支付→取车(20→30)→还车(40→50)→完成(60)，每步校验 canTransition | P0 |
| E2E-03 | 订单取消退款梯度 | 不同时间点取消：48h前全额/24-48h 80%/12-24h 50%/12h内不予退 | P1 |
| E2E-04 | RBAC 权限控制矩阵 | admin/manager/operator 三种角色分别访问各接口的允许/拒绝矩阵 | P0 |
| E2E-05 | 并发分布式锁 | 同时两个请求 acquireLock 同一个 lockKey，仅一者成功 | P1 |

---

## 十四、测试执行说明

### 14.1 技术栈
- **框架**：Jest（NestJS 内置）
- **测试库：`@nestjs/testing`
- **Mock：`jest.mock()` + 手动 mock（Repository/Redis/JwtService）
- **覆盖率工具**：Istanbul（`--coverage`）

### 14.2 启动命令
```bash
# 运行所有测试
npm run test

# 运行特定模块测试
npx jest src/common/guards/ --verbose
npx jest src/modules/auth/ --verbose

# 生成覆盖率报告
npm run test -- --coverage

# 监听模式（开发中常用）
npm run test:watch
```

### 14.3 关键 Mock 策略

| 依赖 | Mock 方式 | 说明 |
|------|----------|------|
| **TypeORM Repository** | `getRepository.mockReturnValue({...})` | 不连接真实 DB |
| **Redis (ioredis)** | `jest.mock('ioredis')` | 内存模式或完全 mock |
| **JwtService (NestJS)** | `jwtService.signAsync.mockResolvedValue('fake-token')` | 不验证签名真实性 |
| **ConfigService** | `configService.get.mockReturnValue('test-secret')` | 固定配置值 |

### 14.4 测试文件命名规范
```
src/
├── common/
│   ├── guards/
│   │   ├── jwt-auth.guard.spec.ts      # JwtAuthGuard 测试
│   │   └── roles.guard.spec.ts         # RolesGuard 测试
│   ├── filters/
│   │   ├── http-exception.filter.spec.ts
│   │   └── query-exception.filter.spec.ts
│   └── redis/
│       └── redis.service.spec.ts
├── modules/
│   ├── auth/
│   │   ├── auth.service.spec.ts
│   │   └── auth.controller.spec.ts
│   ├── order/
│   │   └── order.service.spec.ts       # 重点！状态机逻辑
│   └── ...
```

---

## 六、第二阶段（W5-8）业务逻辑测试用例

> **新增日期**：2026-04-06
> **对应代码**：Order/Payment/Admin/User/Coupon/Message 模块的业务逻辑填充

### 6.1 Order 订单模块 — 业务逻辑测试（22 例）

#### 6.1.1 createOrder — 下单创建（8 例）

| 编号 | 测试场景 | 输入 | 预期输出 | 优先级 |
|------|---------|------|---------|--------|
| O2-01 | 正常下单 | 有效 userId + storeId + vehicleId(可用) + 时间范围 | 返回订单对象，status=PENDING_PAYMENT，orderNo=GY格式，价格已计算 | P0 |
| O2-02 | 车辆不可用下单 | vehicleId 状态为 RENTED | 抛出 BusinessException(VEHICLE_NOT_AVAILABLE) | P0 |
| O2-03 | 重复下单防重 | 相同参数短时间内提交两次 | 第二次返回已存在订单（幂等处理） | P0 |
| O2-04 | 取车时间晚于还车时间 | startTime > endTime | 抛出 ParamError(时间范围无效) | P0 |
| O2-05 | 取车时间为过去时间 | startTime < now() | 抛出 ParamError(取车时间不能是过去) | P1 |
| O2-06 | 多车辆同时下单 | orderItems 含 2 辆车 | 返回含多个 orderItem 的订单，总价正确累加 | P1 |
| O2-07 | 使用优惠券下单 | userCouponId 有效 | 订单 discountAmount 正确抵扣 | P1 |
| O2-08 | 门店不存在 | storeId 不存在 | 抛出 NotFoundException(门店不存在) | P1 |

#### 6.1.2 cancelOrder — 订单取消（4 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| O2-09 | 正常取消(PENDING_PAYMENT状态) | status→CANCELLED, cancelledAt 有值, 车辆库存释放, 发送取消通知 | P0 |
| O2-10 | 已支付订单取消 | status=PAID → CANCELLING (需退款流程) | P0 |
| O2-11 | 非法状态取消(已完成) | status=COMPLETED → 抛错(ORDER_CANNOT_CANCEL) | P0 |
| O2-12 | 非本人取消 | userId ≠ order.userId → 抛错(FORBIDDEN) | P1 |

#### 6.1.3 pickupVehicle / returnVehicle — 取车/还车（6 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| O2-13 | 正常取车(PAID状态) | status→IN_PROGRESS, pickupTime 有值, 车辆状态→RENTED, 发送取车通知 | P0 |
| O2-14 | 未支付取车 | status=PENDING_PAYMENT → 抛错(ORDER_NOT_PAID) | P0 |
| O2-15 | 正常还车(IN_PROGRESS) | status→PENDING_SETTLEMENT, returnTime 有值, 费用已预计算 | P0 |
| O2-16 | 未取车直接还车 | status=PAID → 抛错(VEHICLE_NOT_PICKED_UP) | P1 |
| O2-17 | 并发取车锁测试 | 两个请求同时取车同一订单 | 只有一个成功(分布式锁生效) | P1 |
| O2-18 | 还车费用验证 | 租赁时长 × 日租金 = rentalFee(±误差1分) | P0 |

#### 6.1.4 previewSettlement / confirmSettlement — 结算（4 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| O2-19 | 价格预览 | 返回完整费用明细(rentalFee/serviceFee/insuranceFee/disAmount/totalFee) | P0 |
| O2-20 | 确认还车结算 | status→COMPLETED, completedAt 有值, 积分已发放, 发送还车通知 | P0 |
| O2-21 | 优惠券退还 | 使用过的优惠券在取消时自动退回(USED→AVAILABLE) | P1 |
| O2-22 | 超时还车费 | 实际还车超时 → overtimeFee > 0 | P1 |

---

### 6.2 Payment 支付模块 — 微信支付V3集成测试（14 例）

#### 6.2.1 createWxPayment — 创建支付（4 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| P2-01 | 正常创建微信支付 | 返回 payment 对象 + 微信支付参数(prepay_id等) | P0 |
| P2-02 | 订单不存在 | orderId 无效 → NotFoundException | P0 |
| P2-03 | 订单金额为0 | 免费订单跳过支付直接完成 | P1 |
| P2-04 | 重复支付请求 | 已有成功支付记录 → 返回已有记录(幂等) | P0 |

#### 6.2.2 handleWxNotify — 支付回调（5 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| P2-05 | 正常回调验签通过 | 更新支付状态SUCCESS + 订单状态PAID + 发送支付通知 + 返回 success XML | P0 |
| P2-06 | 签名伪造回调 | 返回 fail(XML) + 不更新任何状态 | P0 |
| P2-07 | 重复回调(幂等) | 相同交易号第二次回调 → 返回 success 但不重复处理 | P0 |
| P2-08 | 回调金额不匹配 | 回调金额 ≠ 记录金额 → 返回 fail + 记录异常日志 | P0 |
| P2-09 | 回调订单不存在 | trade_no 匹配不到订单 → 返回 fail | P1 |

#### 6.2.3 processRefund — 退款（5 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| P2-10 | 全额退款申请 | 创建退款记录(status=PROCESSING) + 调用微信退款API | P0 |
| P2-11 | 部分退款 | refundAmount < paidAmount → 按比例退款 | P1 |
| P2-12 | 退款金额超限 | refundAmount > 可退金额 → 报错(REFUND_AMOUNT_EXCEEDED) | P0 |
| P2-13 | 退款回调处理 | 微信退款成功 → status=REFUND_REFUNDED + 优惠券退还 | P0 |
| P2-14 | 重复退款申请 | 已有进行中退款 → 报错(REFUND_IN_PROGRESS) | P1 |

---

### 6.3 Admin 管理后台测试（12 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| A2-01 | 仪表盘统计 | 返回今日订单数/总用户数/总收入/车辆利用率 | P0 |
| A2-02 | 车辆列表(分页+筛选) | 分页数据 + 状态/门店筛选正常 | P0 |
| A2-03 | 新增车辆 | 创建车辆记录 + 默认状态AVAILABLE | P1 |
| A2-04 | 编辑车辆信息 | 更新成功 + 字段校验 | P1 |
| A2-05 | 门店列表 | 返回所有门店含坐标信息 | P1 |
| A2-06 | 用户管理列表 | 分页 + 手机号搜索 + 注册时间排序 | P1 |
| A2-07 | 订单详情(含关联) | 返回订单 + 用户 + 车辆 + 支付记录 | P0 |
| A2-08 | 优惠券模板列表 | 分页 + 状态筛选(启用/停用/过期) | P1 |
| A2-09 | 停用优惠券 | enabled=false → 已领取的券不受影响 | P1 |
| A2-10 | 系统设置更新 | 更新配置项并持久化 | P2 |
| A2-11 | 权限校验(非管理员) | 普通员工访问管理接口 → 403 Forbidden | P0 |
| A2-12 | 数据导出(CSV) | 订单/用户数据导出为CSV格式 | P2 |

---

### 6.4 User 用户中心测试（10 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| U2-01 | 获取个人信息(脱敏) | phone 显示 138****1234, idCard 不返回 | P0 |
| U2-02 | 更新昵称 | nickname 更新成功 | P1 |
| U2-03 | 获取会员信息 | 返回等级名称/积分/权益列表 | P1 |
| U2-04 | 获取钱包信息 | 余额 + 最近10条收支流水 | P1 |
| U2-05 | 收藏车辆列表 | 返回已收藏的车辆(含车型信息) | P2 |
| U2-06 | 添加收藏(首次) | favorites 表新增记录 | P2 |
| U2-07 | 取消已收藏 | favorites 表删除记录 | P2 |
| U2-08 | 重复收藏 | 幂等处理(不报错也不重复插入) | P2 |
| U2-09 | 获取骑行统计 | 总次数/总时长/总花费/平均单次 | P2 |
| U2-10 | 无骑行历史 | 统计全为0 | P2 |

---

### 6.5 Coupon 优惠券系统测试（14 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| C2-01 | 我的优惠券列表(全部) | 返回 available + used + expired 三态 | P0 |
| C2-02 | 按状态筛选(status=available) | 只返回未使用且未过期的券 | P1 |
| C2-03 | 正常领券 | UserCoupon 新增(status=AVAILABLE), coupon.issuedCount++ | P0 |
| C2-04 | 超限领券(limitPerUser=1, 已领1张) | 报错(COUPON_CLAIM_LIMIT_EXCEEDED) | P0 |
| C2-05 | 券已领完(totalCount=issuedCount) | 报错(COUPON_OUT_OF_STOCK) | P0 |
| C2-06 | 券未开始(validFrom > now) | 报错(COUPON_NOT_YET_AVAILABLE) | P1 |
| C2-07 | 券已过期(validTo < now) | 报错(COUPON_EXPIRED) | P1 |
| C2-08 | 核销优惠券(满足门槛) | UserCoupon status→USED, usedTime有值, 返回折扣金额 | P0 |
| C2-09 | 核销不满足门槛(minAmount=100, 订单80) | 报错(COUPON_MIN_AMOUNT_NOT_MET) | P0 |
| C2-10 | 订单可用券查询 | 返回该订单金额可用的券列表 + 最佳推荐 | P1 |
| C2-11 | 积分获取(下单奖励) | PointsRecord 新增(type=EARN), balance 增加 | P1 |
| C2-12 | 积分消耗(兑换) | balance >= 消耗数 → 扣减; 否则报错 | P1 |
| C2-13 | 积分不足消耗 | 报错(POINTS_INSUFFICIENT) | P1 |
| C2-14 | 优惠券退还(订单取消) | USED→AVAILABLE, usedTime 清空 | P1 |

---

### 6.6 Message 消息通知测试（10 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| M2-01 | 消息列表(分页) | 返回消息数组 + 分页元数据 | P0 |
| M2-02 | 按类型筛选(messageType=order_status) | 只返回订单相关消息 | P1 |
| M2-03 | 标记已读 | isRead=0→1, readAt 有值 | P0 |
| M2-04 | 标记全部已读 | 该用户所有 isRead=0 的消息更新为1 | P1 |
| M2-05 | 未读数量(Redis缓存命中) | 从缓存返回, 不查数据库 | P0 |
| M2-06 | 未读数量(缓存未命中) | 查数据库 + 写入缓存(TTL=300s) | P0 |
| M2-07 | 发送站内信(in_app) | message_log 新增, sendStatus=success | P0 |
| M2-08 | 发送后清除未读缓存 | 缓存 key `msg:unread:{userId}` 被删除 | P1 |
| M2-09 | 下单成功通知模板 | type=order_status, title="下单成功", channel=push | P1 |
| M2-10 | 批量发送(系统公告) | 多个 userId 各写入一条 message_log | P2 |

---

### 第二阶段新增汇总

| 模块 | 新增用例数 | 关键覆盖点 |
|------|-----------|-----------|
| **Order** | **22** | 下单创建/状态机流转/取车还车/并发锁/费用计算 |
| **Payment** | **14** | 微信统一下单/回调验签/幂等/签名伪造/退款流程 |
| **Admin** | **12** | 仪表盘统计/CRUD/权限校验/数据导出 |
| **User** | **10** | 脱敏输出/钱包/收藏/骑行统计 |
| **Coupon** | **14** | 领取校验/核销/积分/退还/最佳推荐 |
| **Message** | **10** | 分页/缓存/已读/批量发送/通知模板 |
| **小计** | **82** | — |

**累计总计：93（第一阶段）+ 82（第二阶段）= 175 个测试用例**

---

## 十五、第三阶段（W9-14）新增模块测试用例

> **新增日期**：2026-04-06
> **对应代码**：8个新模块(store/violation/invoice/wallet/renewal/review/feedback/favorite) + 9个增强模块 + 安全加固

### 15.1 Store 门店模块测试（15 例）

#### 15.1.1 Store CRUD + LBS 查询（10 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| S-01 | 创建门店(合法数据) | 返回门店对象，id有值，isActive=true | P0 |
| S-02 | 创建门店(名称重复) | 报错(STORE_NAME_DUPLICATE) 或唯一约束冲突 | P0 |
| S-03 | 门店列表(分页) | 返回分页结构 list+total，默认按createdAt DESC | P0 |
| S-04 | 按状态筛选(isActive=false) | 仅返回已禁用门店 | P1 |
| S-05 | 门店详情(存在ID) | 返回完整门店信息含坐标 | P0 |
| S-06 | 门店详情(不存在ID) | 抛出 NotFoundException | P0 |
| S-07 | 更新门店信息 | 字段正确更新，updatedAt刷新 | P1 |
| S-08 | 软删除门店 | deletedAt有值，不物理删除 | P1 |
| S-09 | LBS附近门店查询 | 结果按距离升序排列，仅返回指定半径内 | P0 |
| S-10 | 门店库存查询 | 关联vehicle表，返回该门店可用车辆数 | P1 |

#### 15.1.2 门店坐标更新（3 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| S-11 | 更新经纬度 | latitude/longitude 正确更新 | P1 |
| S-12 | 无效坐标(超范围) | latitude > 90 或 < -90 → 校验失败 | P2 |
| S-13 | 无权限更新坐标 | 非管理员角色 → 403 Forbidden | P0 |

#### 15.1.3 门店 DTO 校验（2 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| S-D-01 | 店名必填+长度 | name为空→必填错误；name>100字→MaxLength错误 | P0 |
| S-D-02 | 电话号码格式 | phone不符合手机号格式 → IsPhoneNumber失败 | P1 |

---

### 15.2 Staff 员工管理增强测试（12 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| ST2-01 | 创建员工账号 | 密码bcrypt加密存储，status=ACTIVE | P0 |
| ST2-02 | 员工列表(分页+搜索) | 支持按username/phone搜索 | P0 |
| ST2-03 | 员工详情 | 返回员工信息(password字段不返回) | P0 |
| ST2-04 | 更新员工信息(本人) | 允许修改自己的昵称/手机 | P1 |
| ST2-05 | 禁用员工 | status→INACTIVE，Token失效 | P0 |
| ST2-06 | 启用已禁用员工 | status→ACTIVE | P1 |
| ST2-07 | 分配角色(单个) | staff_role表新增关联记录 | P0 |
| ST2-08 | 分配角色(多个) | 批量写入staff_role，去重 | P1 |
| ST2-09 | 重置密码 | 新密码bcrypt加密，旧Token失效 | P0 |
| ST2-10 | 非管理员创建员工 | operator角色 → 403 Forbidden | P0 |
| ST2-11 | 删除已有关联订单的员工 | 报错(STAFF_HAS_ORDERS) | P1 |
| ST2-12 | 员工名唯一性 | 同名用户名创建第二个 → 冲突错误 | P0 |

---

### 15.3 Finance 财务统计测试（10 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| F-01 | 日收入统计 | 返回当日支付成功金额汇总 | P0 |
| F-02 | 周收入趋势 | 返回7天每日收入数组 | P0 |
| F-03 | 月收入统计 | 返回当月总收入+日均 | P0 |
| F-04 | 自定义区间统计 | startDate~endDate 收入合计 | P1 |
| F-05 | 对账汇总 | 应收vs实收差异报表 | P0 |
| F-06 | 对账流水明细 | 分页支付记录列表含状态 | P0 |
| F-07 | Dashboard概览 | 今日收入/订单数/车辆利用率/活跃用户4指标 | P0 |
| F-08 | Excel导出(收入报表) | 返回.xlsx文件流 | P2 |
| F-09 | 无数据时统计 | 所有数值为0，不报错 | P1 |
| F-10 | 非管理员访问财务 | 403 Forbidden | P0 |

---

### 15.4 Pricing 定价规则测试（15 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| PR-01 | 获取所有定价规则 | 返回所有车型规则列表 | P0 |
| PR-02 | 工作日价格试算 | basePrice × weekdayFactor(1.0) | P0 |
| PR-03 | 周末价格试算 | basePrice × weekendFactor(1.2) | P0 |
| PR-04 | 节假日价格试算 | basePrice × holidayFactor(1.5) | P1 |
| PR-05 | 旺季系数试算 | basePrice × peakSeasonFactor(1.3) | P1 |
| PR-06 | 多日租赁跨周末 | 混合工作日+周末天数分别计价 | P0 |
| PR-07 | 更新定价规则 | 规则值正确更新 | P1 |
| PR-08 | 负数价格校验 | basePrice < 0 → 校验失败 | P0 |
| PR-09 | 系数为0 | factor=0 → 价格为0（边界情况） | P2 |
| PR-10 | 不存在的车型ID | 抛出 NotFoundException | P0 |
| PR-11 | 含保险费计算 | rentalFee + insuranceFee(租金×10%) = totalFee | P0 |
| PR-12 | 含服务费计算 | totalFee + serviceFee(固定5元) = finalTotal | P1 |
| PR-13 | 会员折扣应用 | 金卡用户 finalTotal × 0.9 | P1 |
| PR-14 | 优惠券抵扣 | 满减券直接减；折扣券按比例减 | P0 |
| PR-15 | 最终价格不低于0 | 各项抵扣后 min(total, 0) | P1 |

---

### 15.5 Violation 违章押金测试（8 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| VIO-01 | 还车时自动冻结违章押金 | 新建ViolationDeposit记录，status=FROZEN，amount=500 | P0 |
| VIO-02 | 查询订单违章押金状态 | 返回当前状态和金额 | P0 |
| VIO-03 | 商家发起扣除 | status→DEDUCTED, deductionAmount有值, deductedAt有值 | P0 |
| VIO-04 | 手动退还(无违章) | status→REFUNDED, refundAmount=冻结金额 | P0 |
| VIO-05 | 部分退还 | status→PARTIAL_REFUND, refundAmount<冻结金额 | P1 |
| VIO-06 | 30天观察期自动退还 | 定时任务检查 observationEndAt < now 且无违章 → 自动REFUNDED | P1 |
| VIO-07 | 重复扣除 | 已DEDUCTED状态下再次扣除 → 报错 | P0 |
| VIO-08 | 违章押金列表(商家端) | 分页列表含状态筛选 | P1 |

---

### 15.6 Invoice 发票管理测试（10 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| INV-01 | 新增企业抬头 | name+taxNo必填，taxNo校验格式(X位数字/大写字母) | P0 |
| INV-02 | 新增个人抬头 | 仅name必填，taxNo可选 | P1 |
| INV-03 | 抬头列表 | 返回我的抬头列表 | P0 |
| INV-04 | 编辑抬头 | 仅允许编辑自己创建的抬头 | P1 |
| INV-05 | 删除抬头 | 软删除 | P1 |
| INV-06 | 申请开票(已完成订单) | 新建Invoice记录，status=PENDING | P0 |
| INV-07 | 申请开票(未完成订单) | 报错(INVOICE_ORDER_NOT_COMPLETED) | P0 |
| INV-08 | 发票列表 | 分页含状态筛选 | P0 |
| INV-09 | 发票详情 | 含抬头信息+订单信息+物流信息 | P1 |
| INV-10 | 抬头名称重复 | 同一用户同名抬头 → 提示已存在 | P2 |

---

### 15.7 Wallet 钱包模块测试（12 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| WAL-01 | 查询钱包信息 | 返回balance+frozenAmount+version | P0 |
| WAL-02 | 新用户钱包自动创建 | 首次查询时自动初始化(balance=0, frozen=0) | P0 |
| WAL-03 | 交易流水(分页) | 按时间倒序，含type/amount/balanceAfter | P0 |
| WAL-04 | 流水类型筛选(type=RECHARGE) | 仅返回充值记录 | P1 |
| WAL-05 | 充值(调支付宝) | 创建Payment记录+WalletTransaction(RECHARGE类型) | P0 |
| WAL-06 | 支付扣款 | balance减少，乐观锁version+1 | P0 |
| WAL-07 | 并发扣款安全 | 两个请求同时扣款 → 乐观锁防止超额 | P0 |
| WAL-08 | 余额不足扣款 | 报错(WALLET_BALANCE_INSUFFICIENT) | P0 |
| WAL-09 | 退款入账 | balance增加，生成REFUND类型流水 | P1 |
| WAL-10 | 冻结金额(违章押金) | frozenAmount增加，balance不变 | P1 |
| WAL-11 | 解冻金额(退还) | frozenAmount减少 | P1 |
| WAL-12 | 流水金额符号 | RECHARGE/REFUND为正入账; PAY/DEDUCTION为负出账 | P0 |

---

### 15.8 Renewal 续租模块测试（8 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| REN-01 | 发起续租申请 | 新建Renewal记录，status=PENDING | P0 |
| REN-02 | 我的续租记录 | 返回当前用户的续租列表 | P0 |
| REN-03 | 待审批列表(商家端) | 返回所有PENDING状态的续租申请 | P0 |
| REN-04 | 审批通过 | status→APPROVED，生成补充订单 | P0 |
| REN-05 | 审批拒绝(附原因) | status→REJECTED，rejectReason有值 | P0 |
| REN-06 | 支付续租费用 | 调用支付接口，status→PAID | P0 |
| REN-07 | 非使用中订单不可续租 | 订单非IN_USE状态 → 报错 | P0 |
| REN-08 | 续租时长校验 | 延长时间必须>0且≤30天 | P1 |

---

### 15.9 Member 会员等级测试（10 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| MEM-01 | 新注册用户等级 | 默认普通会员(levelId=1) | P0 |
| MEM-02 | 升级银卡(累计≥2000) | levelId→2，触发升级通知 | P0 |
| MEM-03 | 升级金卡(累计≥8000) | levelId→3 | P0 |
| MEM-04 | 升级钻石(累计≥20000) | levelId→4 | P0 |
| MEM-05 | 降级保护 | 累计消费下降不降级(只升不降) | P1 |
| MEM-06 | 获取会员权益 | 返回对应等级的折扣率/免押额度/积分倍率 | P0 |
| MEM-07 | 升级进度 | 当前累计 / 下一级门槛 的百分比 | P1 |
| MEM-08 | 最高等级不再升级 | 钻石用户调用checkAndUpgrade → 无变化 | P2 |
| MEM-09 | 会员折扣应用 | 下单时根据等级自动应用折扣 | P1 |
| MEM-10 | 积分倍率生效 | 完成订单后积分奖励×倍率 | P1 |

---

### 15.10 Points 积分体系测试（10 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| PT-01 | 查询积分余额 | 返回当前可用积分 | P0 |
| PT-02 | 积分明细(分页) | 返回流水记录含来源类型 | P0 |
| PT-03 | 订单完成获积分 | orderAmount×1%，上限500分/单 | P0 |
| PT-04 | 每日签到 | +5积分/天，重复签到报错 | P0 |
| PT-05 | 今日签到状态查询 | 返回true/false | P1 |
| PT-06 | 评价获积分 | 文字评价20分，图文额外+10分 | P1 |
| PT-07 | 邀请注册获积分 | +100分/人 | P2 |
| PT-08 | 积分消耗(兑换) | balance >= cost → 扣减并记录 | P1 |
| PT-09 | 积分不足消耗 | 报错(POINTS_INSUFFICIENT) | P0 |
| PT-10 | 积分规则查询 | 返回4种获取渠道配置 | P2 |

---

### 15.11 Coupon 优惠券完整流程测试（15 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| CO3-01 | 创建满减券模板 | type=FIXED_AMOUNT, minAmount/discountAmount设置 | P0 |
| CO3-02 | 创建折扣券模板 | type=PERCENTAGE, discountPercent/maxDiscount设置 | P0 |
| CO3-03 | 创建新人券模板 | isFirstOnly=true | P1 |
| CO3-04 | 创建无门槛券 | minAmount=0 | P1 |
| CO3-05 | 模板列表(商家端) | 分页含状态筛选 | P0 |
| CO3-06 | 编辑未发放模板 | 修改规则值成功 | P1 |
| CO3-07 | 编辑已发放模板 | 报错(COUPON_ALREADY_ISSUED) | P0 |
| CO3-08 | 用户领券 | UserCoupon新建(status=AVAILABLE)，模板issuedCount+1 | P0 |
| CO3-09 | 领券超限(limitPerUser) | 已领N张再领 → 报错 | P0 |
| CO3-10 | 券已发完(totalCount=issuedCount) | 报错(COUPON_OUT_OF_STOCK) | P0 |
| CO3-11 | 核销满减券 | orderAmount>=minAmount → discount=discountAmount | P0 |
| CO3-12 | 核销折扣券 | discount=orderAmount×(1-discountPercent/100)，不超过maxDiscount | P0 |
| CO3-13 | 核销新人券 | 校验isFirstOrder=true | P1 |
| CO3-14 | 核销过期券 | validTo < now → 报错(COUPON_EXPIRED) | P0 |
| CO3-15 | 可用券推荐 | 返回满足minAmount+有效期+状态的券，按优惠力度排序 | P1 |

---

### 15.12 Review 评价模块测试（8 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| RV-01 | 提交评价(已完成订单) | Review新建，rating/content/tags/images保存 | P0 |
| RV-02 | 未完成订单评价 | 报错(ORDER_NOT_COMPLETED) | P0 |
| RV-03 | 重复评价(同一订单) | 报错(REVIEW_ALREADY_EXISTS) — 每订单限评1次 | P0 |
| RV-04 | 车辆评价列表(分页) | 返回该车辆的所有评价(审核通过的) | P0 |
| RV-05 | 评分汇总(均值+分布) | 返回averageRating + 1-5星各占比 | P0 |
| RV-06 | 我的评价列表 | 返回当前用户的评价 | P1 |
| RV-07 | 商家回复评价 | replyContent+repliedAt保存 | P1 |
| RV-08 | 敏感词过滤 | 含敏感词的评价 → status=PENDING(待审核) | P1 |

---

### 15.13 Favorite 收藏模块测试（6 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| FAV-01 | 收藏车辆 | favorite表新增记录 | P0 |
| FAV-02 | 重复收藏(幂等) | 不报错，不重复插入，返回已收藏 | P0 |
| FAV-03 | 取消收藏 | 删除favorite记录 | P0 |
| FAV-04 | 取消不存在的收藏 | 不报错(幂等) | P2 |
| FAV-05 | 我的收藏列表(分页) | 返回含车辆信息的收藏列表 | P0 |
| FAV-06 | 检查是否已收藏 | 返回boolean | P1 |

---

### 15.14 Feedback 反馈模块测试（5 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| FB-01 | 提交反馈 | Feedback新建，category/content/contact保存 | P0 |
| FB-02 | 反馈列表(我的) | 分页返回当前用户的反馈 | P0 |
| FB-03 | 反馈详情 | 返回含处理结果的详情 | P1 |
| FB-04 | 分类校验 | category必须在预定义枚举内 | P0 |
| FB-05 | 图片附件(可选) | images字段可存URL数组 | P2 |

---

### 15.15 Ticket 工单系统测试（20 例）

#### 15.15.1 用户端工单操作（6 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| TK-01 | 提交工单 | Ticket新建，category/priority/content/images保存，status=PENDING | P0 |
| TK-02 | 我的工单列表(分页) | 返回当前用户的工单 | P0 |
| TK-03 | 工单详情(含回复) | 返回工单+ticketReply列表 | P0 |
| TK-04 | 追加回复 | TicketReply新建，如果当前PROCESSING则保持 | P1 |
| TK-05 | 分类校验 | category必须在TicketCategory枚举内 | P0 |
| TK-06 | SLA自动分配priority | 根据category自动设定响应时限 | P1 |

#### 15.15.2 商家端工单处理（8 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| TK-07 | 工单全量列表(商家端) | 分页+分类/状态/优先级筛选 | P0 |
| TK-08 | 分派员工 | assignStaffId更新 | P0 |
| TK-09 | 状态变更(PENDING→PROCESSING) | 合法转换 | P0 |
| TK-10 | 状态变更(PROCESSING→RESOLVED) | 合法转换 | P0 |
| TK-11 | 非法状态变更(RESOLVED→PENDING) | 报错(ILLEGAL_STATUS_TRANSITION) | P0 |
| TK-12 | 商家回复 | TicketReply(senderType=STAFF) | P0 |
| TK-13 | 拒绝工单 | status→REJECTED，rejectReason有值 | P1 |
| TK-14 | 重新打开已关闭工单 | CLOSED→REOPENED→PROCESSING | P1 |

#### 15.15.3 SLA升级机制（4 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| TK-15 | L1→L2升级(超响应时限) | 处理中超过SLA.response时间未回复 → 升到L2 | P1 |
| TK-16 | L2→L3升级(超解决时限×2) | 升到L3 | P1 |
| TK-17 | L3→L4升级(超解决时限×3) | 升到L4 | P2 |
| TK-18 | L4超48h标记异常 | status→ABNORMAL + 发送短信通知负责人 | P2 |

#### 15.15.4 工单状态机（2 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| TK-19 | 9种状态全部合法转移验证 | 按状态机图逐一验证 | P0 |
| TK-20 | 终态不可转移 | CLOSED/REJECTED 无法转移到其他状态 | P0 |

---

### 15.16 Security 安全加固测试（10 例）

#### 15.16.1 数据脱敏测试（5 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| SEC-01 | 手机号脱敏 | 13812345678 → 138****5678 | P0 |
| SEC-02 | 身份证脱敏 | 110101199001011234 → 110101********1234 | P0 |
| SEC-03 | 姓名脱敏 | 张三 → 张* | P0 |
| SEC-04 | 银行卡脱敏 | 622202***9012 | P1 |
| SEC-05 | 驾驶证号脱敏 | 同身份证规则 | P1 |

#### 15.16.2 接口限流测试（5 例）

| 编号 | 测试场景 | 预期输出 | 优先级 |
|------|---------|---------|--------|
| TH-01 | IP级别全局限制(100次/分钟) | 第101次请求 → 429 Too Many Requests | P0 |
| TH-02 | 用户级别限制(30次/分钟) | 第31次请求 → 429 | P0 |
| TH-03 | 登录接口(5次/15分钟) | 第6次 → 429 | P0 |
| TH-04 | 发送验证码(1次/60秒) | 第2次 → 429 | P0 |
| TH-05 | 窗口重置 | 等待窗口期后再请求 → 正常放行 | P1 |

---

### 第三阶段新增汇总

| 模块 | 新增用例数 | 关键覆盖点 |
|------|-----------|-----------|
| **Store 门店** | **15** | CRUD/LBS查询/坐标更新/DTO校验 |
| **Staff 员工** | **12** | CRUD/角色分配/权限/密码重置 |
| **Finance 财务** | **10** | 收入统计/对账/Dashboard/导出 |
| **Pricing 定价** | **15** | 规则CRUD/多因素计价/折扣叠加 |
| **Violation 违章** | **8** | 冻结/扣除/退还/观察期/部分退款 |
| **Invoice 发票** | **10** | 抬头管理/申请流程/税号校验 |
| **Wallet 钱包** | **12** | 余额/流水/并发安全/乐观锁 |
| **Renewal 续租** | **8** | 申请/审批/支付/状态机 |
| **Member 会员** | **10** | 4级等级/升级判定/权益/折扣 |
| **Points 积分** | **10** | 4渠道获取/签到/消耗/规则 |
| **Coupon 增强** | **15** | 模板管理/领取/核销/新人礼包 |
| **Review 评价** | **8** | 提交/汇总/回复/敏感词 |
| **Favorite 收藏** | **6** | 收藏/取消/幂等/列表 |
| **Feedback 反馈** | **5** | 提交/查询/分类校验 |
| **Ticket 工单** | **20** | 全生命周期/状态机(9态)/SLA升级(4级) |
| **Security 安全** | **10** | 5类脱敏/5级限流 |
| **小计** | ****175**** | — |

**三阶段累计总计：175（第一二阶段）+ 175（第三阶段）= 350 个测试用例**

---

*本文档由 AI 于 2026-04-05 创建，2026-04-06 更新第三阶段，共 350 个单元测试用例 + 5 个集成测试场景。随每次编码迭代持续补充。*
